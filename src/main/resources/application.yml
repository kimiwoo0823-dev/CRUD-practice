spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${DB_NAME:board2}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:am02060710@}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false
